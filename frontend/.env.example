# 前端端口 (默认 5173，可修改)
VITE_PORT=5173

# 后端 API 地址
VITE_API_URL=http://localhost:3001/api

# 允许访问的主机 (Vite 开发服务器)
# true = 允许所有主机访问 (方便但不安全)
# 逗号分隔列表 = 只允许指定主机 (推荐)
#   例:localhost,127.0.0.1
# 生产环境建议填写实际域名，不要用 true
VITE_ALLOWED_HOSTS=localhost
