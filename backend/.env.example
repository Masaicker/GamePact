# 服务器配置
PORT=3001
# 开发模式 (development) 或 生产模式 (production)
NODE_ENV=development

# 数据库配置 (本地开发使用 SQLite)
DATABASE_URL="file:./dev.db"

# JWT 密钥 (setup.ps1 会自动为您生成一个强密码，无需手动修改)
JWT_SECRET=gamepact-dev-secret-key-placeholder

# CORS 配置 (允许访问的前端地址)
# 本地开发填: http://localhost:5173
# 云服务器建议填: * (允许所有，最简单) 或 http://<公网IP>:5173
CORS_ORIGIN=*